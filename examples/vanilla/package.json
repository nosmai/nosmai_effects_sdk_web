{
  "name": "nosmai-web-vanilla-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:assets": "node ../copy-nosmai-assets.mjs public/nosmai",
    "dev": "npm run prepare:assets && vite --port 5180",
    "build": "npm run prepare:assets && tsc --noEmit && vite build"
  },
  "dependencies": {
    "@nosmai/web": "file:../.."
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
